# Keep `npm pack` stdout to the tarball filename only. pre-commit's node-language
# install runs `npm pack` and parses stdout for the tarball name; the `prepare`
# build script's npm lifecycle banners would otherwise pollute that stdout and
# break the hook install. This affects npm only (the repo's own workflow uses pnpm).
loglevel=silent
